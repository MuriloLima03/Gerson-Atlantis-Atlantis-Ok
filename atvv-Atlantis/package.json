{
  "dependencies": {
    "@types/node": "^18.0.0",
    "@types/prompt-sync": "^4.1.1",
    "prompt-sync": "^4.2.0",
    "express": "^4.18.4",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "mongoose": "^7.5.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start:api": "npm run build && node src/server/server.js",
    "start:api:dev": "ts-node-dev --respawn --transpile-only src/server/server.ts"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "ts-node-dev": "^2.0.0"
  }
}
